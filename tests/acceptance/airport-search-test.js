import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | airport search', function(hooks) {
  setupApplicationTest(hooks);

  test('should display all important information about the found airport', async function(assert) {
  });
  test('should be able to search airports by name, city name and IATA code', async function(assert) {
  });

});
